<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ai.codriverlabs</groupId>
        <artifactId>microvm-operator-parent</artifactId>
        <version>1.0.18</version>
    </parent>

    <artifactId>operator-controller</artifactId>
    <name>KubeMicroVM - Controller</name>
    <description>JOSDK reconcilers, event sources, AWS SDK integration</description>

    <dependencies>
        <!-- Internal module dependency -->
        <dependency>
            <groupId>ai.codriverlabs</groupId>
            <artifactId>operator-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- SPI — extension interfaces for Community/PRO parity -->
        <dependency>
            <groupId>ai.codriverlabs</groupId>
            <artifactId>operator-spi</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Webhook handlers — validates + mutates MicroVM/pod admission requests.
             Co-located in the same Quarkus app/pod for simplicity at GA scale (≤100 VMs).
             Split into a separate Deployment for high-throughput scenarios (>100 VMs). -->
        <dependency>
            <groupId>ai.codriverlabs</groupId>
            <artifactId>operator-webhook</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Generated AWS Lambda MicroVMs SDK client -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>lambdamicrovms</artifactId>
            <version>${aws.sdk.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache5-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>lambdacore</artifactId>
            <version>${aws.sdk.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache5-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Quarkus Operator SDK -->
        <dependency>
            <groupId>io.quarkiverse.operatorsdk</groupId>
            <artifactId>quarkus-operator-sdk</artifactId>
        </dependency>
        <!-- RBAC annotation support for extra ClusterRole rules -->
        <dependency>
            <groupId>io.quarkiverse.operatorsdk</groupId>
            <artifactId>quarkus-operator-sdk-annotations</artifactId>
            <version>7.8.0</version>
        </dependency>

        <!-- Quarkus CDI -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>

        <!-- Kubernetes Client -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-kubernetes-client</artifactId>
        </dependency>

        <!-- commons-compress optional deps — required for GraalVM native-image.
             Quarkus deployment extensions register CompressorStreamFactory for linking,
             which has hard bytecode refs to these classes. Without them native-image fails
             with NoClassDefFoundError. Not used at runtime. -->
        <dependency>
            <groupId>org.tukaani</groupId>
            <artifactId>xz</artifactId>
        </dependency>
        <dependency>
            <groupId>org.brotli</groupId>
            <artifactId>dec</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.luben</groupId>
            <artifactId>zstd-jni</artifactId>
        </dependency>

        <!-- Health checks -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>

        <!-- Metrics -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- AWS STS via Quarkiverse -->
        <dependency>
            <groupId>io.quarkiverse.amazonservices</groupId>
            <artifactId>quarkus-amazon-sts</artifactId>
        </dependency>

        <!-- AWS Service Quotas — for runtime quota discovery -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>servicequotas</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache5-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- AWS URL Connection Client -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>

        <!-- Kubernetes manifest + container image + Helm chart generation -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-kubernetes</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-container-image-docker</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkiverse.helm</groupId>
            <artifactId>quarkus-helm</artifactId>
            <version>1.4.3</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5-mockito</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>${jqwik.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Jandex index — this module is the Quarkus *application* in this build, so
                 Quarkus indexes it as the root and needs no jar index here. But it is a
                 plain compile dependency of KubeMicroVM-PRO's operator-pro-dist, and
                 without META-INF/jandex.idx ArC silently discovers none of the reconcilers
                 or Default* SPI beans in it — producing a PRO operator missing the entire
                 Community product, with a green build.
                 Version and execution configured in the parent pluginManagement.
                 See docs/design/pro-artifact-consumability.md -->
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Override JOSDK's validating-clusterrolebinding.yaml to reference the renamed role,
                 and add the ClusterIssuer for cross-namespace gateway TLS (RC-2 fix).
                 NOTE: overlay templates (cluster-issuer.yaml etc.) are now included via
                 quarkus.helm.input-directory=src/main/helm in application.properties,
                 so no post-processing copy or helm repackage step is needed here. -->
            <!-- Publish Helm overlay files as a Maven artifact so KubeMicroVM-PRO can
                 declare a versioned dependency instead of vendoring them manually.
                 Artifact: ai.codriverlabs:operator-controller:<version>:zip:helm-templates
                 PRO consumers: use maven-dependency-plugin:unpack to extract.
                 See docs/design/pro-artifact-consumability.md (PRO G17). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.8.0</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/helm-templates.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>true</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>helm-templates-zip</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- jib: cross-platform build without local Docker daemon.
             Used for local dev builds targeting the EKS arm64 nodes.
             Usage: ./mvnw package -Pjib -Dquarkus.container-image.push=true
             Also activated by build-local.sh with the push flag. -->
        <profile>
            <id>jib</id>
            <activation>
                <property><name>jib</name></property>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>io.quarkus</groupId>
                    <artifactId>quarkus-container-image-jib</artifactId>
                </dependency>
            </dependencies>
            <properties>
                <quarkus.container-image.builder>jib</quarkus.container-image.builder>
                <!-- Build for both arches so the image runs on amd64 CI and arm64 Graviton nodes -->
                <quarkus.jib.platforms>linux/amd64,linux/arm64</quarkus.jib.platforms>
            </properties>
        </profile>

        <!-- native: GraalVM native image build.
             Used by CI (per-arch runners) and build-local.sh with the native flag. -->
        <profile>
            <id>native</id>
            <activation>
                <property><name>native</name></property>
            </activation>
            <properties>
                <quarkus.native.enabled>true</quarkus.native.enabled>
                <!-- container-build=false: native binary built on host, not inside Docker -->
                <quarkus.native.container-build>false</quarkus.native.container-build>
            </properties>
        </profile>
        <!--
          Offload the GraalVM native build to remote workers, one per architecture, via the
          scaleout-build control plane. Activate with the scaleout profile.

          Quarkus must emit its native-image sources first, because a Quarkus native build is not a plain
          native-image invocation: augmentation generates a Quarkus feature registration plus reflection
          and resource configuration that cannot be reconstructed from a classpath alone. The plugin
          detects target/native-sources and reuses the argument file Quarkus wrote, rather than deriving
          one of its own.

              ./mvnw -B -pl operator-controller -am package -Pscaleout \
                  -Dquarkus.native.enabled=true -Dquarkus.native.sources-only=true

          The developer needs no AWS permissions beyond invoking the control-plane function: no ECS, S3,
          CloudWatch or ECR access at all.
        -->
        <profile>
            <id>scaleout</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>ai.codriverlabs</groupId>
                        <artifactId>scaleout-build-maven-plugin</artifactId>
                        <version>1.0.0-SNAPSHOT</version>
                        <executions>
                            <execution>
                                <id>native-image-both-architectures</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <buildKinds>
                                <buildKind>native</buildKind>
                            </buildKinds>
                            <architectures>
                                <architecture>x86_64</architecture>
                                <architecture>arm64</architecture>
                            </architectures>
                            <!-- Both cells remotely, including the one matching this host. -->
                            <forceRemote>true</forceRemote>
                            <!-- A real Quarkus app takes far longer than a hello world. -->
                            <timeoutMinutes>25</timeoutMinutes>
                            <overallTimeoutMinutes>40</overallTimeoutMinutes>
                            <endpoint>https://26usidrly3gc4uo4b2i6fcp5ui0njbjo.lambda-url.eu-west-1.on.aws/</endpoint>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
