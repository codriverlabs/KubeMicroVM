KubeMicroVM Operator installed.

Operator pod:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name=operator-controller

CRDs installed:
  kubectl get crd | grep lambda.aws.amazon.com

AWS IAM Setup:
{{- $irsaArn := index (.Values.serviceAccount.annotations | default dict) "eks.amazonaws.com/role-arn" }}
{{- if $irsaArn }}
  IRSA role ARN configured: {{ $irsaArn }}
{{- else }}
  No IAM role annotation configured. Choose one:

  Option 1 - EKS Pod Identity (recommended):
    aws eks create-pod-identity-association \
      --cluster-name <CLUSTER> \
      --namespace {{ .Release.Namespace }} \
      --service-account kube-microvm-operator \
      --role-arn arn:aws:iam::<ACCOUNT_ID>:role/kube-microvm-operator

  Option 2 - IRSA:
    helm upgrade kube-microvm-operator <chart> \
      --set 'serviceAccount.annotations.eks\.amazonaws\.com/role-arn=arn:aws:iam::<ACCOUNT_ID>:role/kube-microvm-operator'
{{- end }}
